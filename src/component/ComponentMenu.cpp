/*#include <ucurses/component/ComponentMenu.hpp>

namespace ucurses {


	void ComponentMenu::Process(int input)
	{
		items[selection]->Process(input);	// Only the selected widget receives input
	}

	void ComponentMenu::Draw()
	{
		for (auto& component : items)
		{
			component->Draw();
		}
	}
}
*/
